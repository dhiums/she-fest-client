mutation PublishResults($programCodes: [String!]! $zone : String!) {
  publishResults(programmeCode: $programCodes zone:$zone){
    id
    resultPublished
    programCode
  }
}

mutation PublishResult($programCodes: String! $zone : String!) {
  publishResult(programmeCode: $programCodes zone:$zone)
}

